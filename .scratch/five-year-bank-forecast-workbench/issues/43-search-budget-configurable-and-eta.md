# 43 — P2：搜索预算可配与耗时预估

**What to build:** 把搜索预算暴露给用户并给出耗时预期：
1. **预算可配**：single/multi 模块的运行区（顶栏按钮旁或变量卡底部，取布局简单者）加"最大测算次数"数字输入：v1 默认 25（下限 3）、v2 默认 15（范围 2–20，与 reverse_calculation.py 硬限制一致）；提交时随请求体发送 `max_evaluations`（替换 app.js:1067/1168 的硬编码）。
2. **耗时预估**：按当前引擎模式显示预估——warm_com 约 0.6–1.5 秒/次、cold_com 约 8–19 秒/次（来自 ticket 15/11 实测，写死在文案中即可），预算 × 单次得出区间，如"预计 15–38 秒"；cold 模式且预估超 15 秒（PRD 上限）时 amber 提示"超出 PRD 5–15 秒预期，建议改用 warm 引擎或降低预算"。
3. 输入校验：越界时 clamp 到合法范围并提示。

**Blocked by:** #36

**Status:** ready-for-agent

- [ ] v1/v2 最大测算次数可配（默认 25/15，范围 3+/2–20），随请求发送
- [ ] 按引擎模式显示耗时预估区间；cold 超 15 秒给 amber 提示
- [ ] 越界输入 clamp 并提示
- [ ] 后端 2–20/≥3 校验报错能原样呈现（不新增后端改动）
- [ ] `python -m unittest discover -s tests` 全绿；`node --check web/app.js` 通过

## Comments

- 2026-07-20 来源：反向模块走查（agent-13 报告摩擦点 #7，agent-14 报告 §1 成本模型）。PRD 允许反向 5–15 秒、>3 秒必须有进度与取消；cold 最坏 25×19s≈8 分钟，用户必须知情。
- 2026-07-20 依赖说明：运行区布局在 #36/#39 动过，本票最后做避免三次重写同一区域。
