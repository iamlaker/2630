# 39 — P1：反向模块术语统一与运行按钮单点化

**What to build:** 收敛反向模块的术语与入口：
1. **术语统一**：动作动词全站统一为"求解"（开始求解/求解中/求解完成），模块名词保持页签"单变量反向/多输入反推"；清理"反向测算/反推/求解/搜索"混用——编辑器内"开始单变量求解/开始多输入求解"、v1 面板"开始反向测算/开始 v2 反推"、进度阶段文案"v1 单变量搜索 第 N 次/v2 优先级搜索 第 N 次"（`stageLabel`，web/app.js:431-434）等统一口径。后端 reason 文案不动（测试逐字锁定）。
2. **运行按钮单点化**：只保留顶栏一个运行按钮（文案随模块切换为"开始求解"），删除编辑器内与 v1 面板里的重复运行按钮（`runReverse`/`runReverseV2` 的额外入口）。按钮 title 在禁用时说明原因（衔接 ticket 36 的解禁逻辑）。
3. 进度文案改为"求解中 第 N/M 次测算"。

**Blocked by:** #36

**Status:** ready-for-agent

- [ ] single/multi 模块全站只有一个运行入口（顶栏），文案"开始求解"
- [ ] 编辑器与 v1 面板内的重复运行按钮移除，对应事件绑定清理
- [ ] 进度阶段文案统一为"求解中 第 N/M 次测算"口径
- [ ] 后端 reason 文案与导出工作表名不变（测试锁定项不碰）
- [ ] `python -m unittest discover -s tests` 全绿；`node --check web/app.js` 通过

## Comments

- 2026-07-20 来源：反向模块走查（agent-13 报告摩擦点 #4、#5）。四套说法并存、同一动作三个按钮文案互不相同。
- 2026-07-20 依赖说明：#36 修解禁逻辑后本票做入口收敛，顺序执行避免同一函数（setCalculateEnabled/setRunning）双人改写。
