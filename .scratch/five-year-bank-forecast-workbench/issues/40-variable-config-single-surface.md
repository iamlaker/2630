# 40 — P1：变量配置单面化——删除 v2 面板、卡面全字段、修配置漂移

**What to build:** 收敛变量配置的双编辑面并修复配置丢失：
1. **卡面全字段**：multi 变量卡在现有（下限/年份/上限/初始值滑杆/优先级）基础上补齐：步长、联动策略（下拉，选项同规则策略）、删除按钮——与现 v2 面板行（`renderReverseVariables`，web/app.js:1119-1150）字段对等。优先级改为 P1/P2/P3 步进器（−/+），卡面附一行说明"先动 P1，不够再动 P2"。
2. **删除 v2 面板**：编辑器内"多输入优先级反推 v2"面板（app.js:850 附近动态插入）整体移除，其变量列表/添加表单删除；"加入当前输入"入口由 ticket 38 的卡面按钮取代。
3. **修单变量配置漂移**：`ensureSingleVariable`（app.js:1382-1399）在换选指标时按新指标静默重建默认值，原变量的范围设置被丢弃。改为：`draft.singleVariable` 一旦由用户修改过（范围/初始值与默认值不同即视为已配置），换选指标时保留原变量配置并提示"当前变量为 X，点击卡片可切换为 Y"；未修改过时维持现状跟随。
4. "年份/年度"等字段命名统一为"年份"。

**Blocked by:** #38

**Status:** ready-for-agent

- [ ] multi 变量卡卡面可编辑：优先级步进、年份、初始值、下限、上限、步长、联动策略、删除
- [ ] v2 面板及其添加表单/变量列表从编辑器移除，无残留引用
- [ ] single 变量已配置后换选指标不丢范围设置，并有切换提示
- [ ] 字段命名统一"年份"
- [ ] 提交体字段（priority/year/initial/lower/upper/step/linkage_strategy）不变
- [ ] `python -m unittest discover -s tests` 全绿；`node --check web/app.js` 通过

## Comments

- 2026-07-20 来源：反向模块走查（agent-13 报告摩擦点 #6、#15）。双编辑面字段不对等；配置漂移属静默丢用户输入。
- 2026-07-20 依赖说明：#38 先把"添加为变量"入口搬上卡面，本票再删 v2 面板，保证任何时刻都有可用入口。
