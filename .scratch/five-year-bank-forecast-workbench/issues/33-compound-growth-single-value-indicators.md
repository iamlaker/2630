# 33 — 复合增速类指标单值维护（仅 2026）

**What to build:** 名称含"复合增速"的输入指标（资本假设（二）（三）节为主）按"单值指标"处理：只维护 2026 一个值（模板中真实输入格在 D 列，2027-2030 为公式），规则确认接受仅 2026 的源单元格为完整，前端卡片只呈现 2026 单值编辑，提交与校验只覆盖 2026，其余年份不强校验、不写入。

**Blocked by:** 无

**Status:** ready-for-agent

- [x] 规则发现/确认：复合增速类指标的候选源单元格只有 2026 时不判"不完整"（现行 `len(year_cells)==len(YEARS)` 才 high confidence 的逻辑对单值指标放宽）
- [x] 规则配置增加单值标记（如 `active_years: [2026]` 或等效），复合增速指标确认时默认带上
- [x] 正向卡片：单值指标只显示/编辑 2026（不出 5 个滑杆），明确标注"全期通用（2026 维护）"
- [x] 提交测算/校验：只提交与校验 2026 值，2027-2030 不要求、不写入
- [x] 基准为空（None）的年份不再参与滑杆范围推导（单值指标天然规避）
- [x] 后端补测试；`python -m unittest discover -s tests` 全绿；`node --check web/app.js` 通过

## Comments

- 2026-07-19 来源：用户反馈第 1 条。模板证据：汇总展示表 176-186 行"复合增速"指标仅 D 列（2026）为输入格。规则证据：rule_versions 中 6.对公贷款承诺复合增速（行 181）pending_confirmation、配置全空。
- 2026-07-19 实现：`_active_years`（名称含"复合增速"→[2026]）；参数下发 `active_years`；`calculate` 过滤提交/校验/写入只保留 2026（保护 2027-2030 公式格）；卡片与编辑器只渲染活跃年份并标注"全期通用 · 仅维护 2026"。确认链路未改动：`configuration_pending` 只看 adjustment_mode+linkage_strategy，2026 单源确认本就不被卡（confidence 仍为 low，仅提示）。顺带修复：未发布但已挂起的规则在导航中显示"待确认"而非"不支持"（latest summaries 回退，canEdit 仍只认已发布规则）。新增 2 项后端测试 + 1 项 UI hooks 测试；169 项全绿；CDP 截屏验证单值卡片（1 轨 + 标注）。
