# 18 — 正向测算与 C–J 结果表

**What to build:** 将正向单场景测算接入新的卡片工作区和结果区。健康时默认使用 `warm_com` 自动模式，输入完成后 500ms 防抖，只计算最新草稿；失败时依次降级为 warm 手动和 cold 手动。右侧输出表严格展示 Excel C–J 契约：C 为 2025 基期，D–H 为 2026–2030，I 为五年变化，J 为 CAGR；I/J 空白保持空白。结果列可拖动调宽，默认优先完整显示数字并压缩指标列。

**Blocked by:** 17 — 指标导航、卡片编辑与展示偏好

**Status:** ready-for-human

- [x] 正向测算默认进入健康的 warm 自动模式，500ms 防抖并合并连续修改
- [x] 运行中只保留最新草稿，右侧继续显示上一次有效结果
- [x] warm 自动失败、warm 手动失败时按约定逐级降级并展示原因
- [x] 导入和结果 API 校验 C–J 列位置与表头，结构异常阻止继续
- [x] 结果表支持 2025–2030、五年变化和 CAGR，I/J 空白不补算
- [x] 百分比、亿元及其他单位按 PRD 格式显示，Excel 导出保留原始精度
- [x] 九个结果列均可拖动宽度，默认数据完整可读，窄栏可横向滚动

## Comments

- 2026-07-19 实现完成：warm 自动模式采用 500ms 防抖；运行中仅标记最新草稿，当前任务结束后只提交最新版本；失败依次降级为 warm 手动和 cold 手动并显示原因。
- Excel 边界改为读取 A:J；导入严格校验 D–H 年份和 I 变化列语义，C/J 允许活动模板使用的空/合并表头；2025、五年变化和 CAGR 均原样透传，空值不补算。结果九列支持拖动宽度和横向滚动。
