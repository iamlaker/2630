# 31 — 输出栏重构：删摘要卡、结果表保留 Excel 层次、可折叠带星标

**What to build:** 右栏输出区重构：删除常驻的核心结果摘要小卡片（#cards 中 core_results 部分）；C–J 完整指标结果表严格按 Excel`汇总展示表`原始行序与原始指标名称展示，并保留总括标题行（一、盈利结果、（一）并表口径、（二）本行口径、二、一级核心指标、三、二级核心指标、四、其他指标、五、风险加权资产增量）作为层次标题；去掉行内"未知 · 财务结果"式的单位·分组尾巴；结果表按标题分节可折叠、指标行可星标（与左栏输入导航同构）。

**Blocked by:** 无

**Status:** ready-for-agent

- [x] 删除右栏常驻摘要小卡片（core_results 卡片）；#cards 容器保留给反向/对比等瞬态结果（无内容时隐藏，不占位）
- [x] 目录/结果数据补"标题行"类型：导入时保留输出区标题行（汇总展示表 4/5/10/15/20/32/53 行）及其行序；result_rows 携带层次信息（header 行 + 指标行的原序）
- [x] 结果表渲染：标题行显示为节标题（可折叠该节）；指标行严格按 Excel 行序、原始名称（含"其中："等缩进语义，可用缩进表达层次）
- [x] 去掉行内 `${unit} · ${group}` 尾巴；"· 较基准变化 / · 已设约束"标记保留
- [x] 指标行支持星标（★），星标状态持久化；折叠节时星标行可留外（与 ticket 28 行为一致）
- [x] 行标记（changed/constraint）与负红正绿着色在新结构下保留
- [x] Edge headless 截屏验证：结果表有节标题、顺序与 Excel 一致、无摘要小卡片
- [x] `python -m unittest discover -s tests` 全绿；解析与行构建补测试

## Comments

- 2026-07-19 来源：用户走查反馈第 3 条。模板证据：标题行在 4/5/10/15/20/32/53 行。注意 result_rows 目前由 `_result_rows(catalog, ...)` 构建、前端 `renderDetails` 渲染表格；ticket 21 的行标记/格式化逻辑要保留。
- 2026-07-19 实现：解析器新增 `section_level` 并按标题行输出 `sections`（真实 COM 解析与目录再生成 `regenerate_catalog`）；`_result_rows` 按 sheet 行序合流 header/row；前端 `renderDetails` 重写为分节可折叠（`resultSections` 草稿持久化、折叠时星标行留外、搜索忽略折叠）、指标行星标（`resultFavorites` 持久化）、删除单位·分组尾巴；常驻摘要卡删除，`#cards` 仅承载反向/对比瞬态结果（`setCards` 管理空态隐藏）；输出导航/过滤跳过标题行；顺带剔除混入目录的"指标"表头行。TDD：新增 3 项后端测试 + 1 项 UI hooks 测试；166 项全绿；CDP 截屏验证分节、L1 折叠 62→52 行、星标留外。
