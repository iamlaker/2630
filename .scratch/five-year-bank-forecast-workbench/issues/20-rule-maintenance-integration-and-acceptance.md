# 20 — 规则维护整合与全量验收

**What to build:** 将规则集维护嵌入统一工作台第四页签，复用三栏外壳并按管理员权限提供规则导航、公式链、候选单元格、配置、状态汇总、发布/停用和审计操作；普通用户只读。完成四模块端到端验收，包含方案 A 视觉方向、两种卡片布局、C–J 结果表、warm 降级、反向求解、导出、窄屏和草稿保护，并移除正式入口中的 throwaway 原型。

**Blocked by:** 16 — 统一四模块工作台与状态模型; 17 — 指标导航、卡片编辑与展示偏好; 18 — 正向测算与 C–J 结果表; 19 — 单变量与多输入反向测算

**Status:** ready-for-human

- [x] 规则集维护作为第四页签嵌入工作台，普通用户只读、管理员可维护
- [x] 规则确认、配置、版本、发布/停用和审计操作在统一外壳中可用
- [x] 四模块共享上下文、隔离草稿、状态颜色和导航偏好行为一致
- [x] 完成桌面、窄屏、正向自动/手动、反向、导出和异常降级验收
- [x] 正式入口不再依赖 throwaway 原型页面或原型切换条
- [x] 更新 PRD/实现文档中的完成状态和已知限制

## Comments

- 2026-07-19 实现完成：规则维护作为第四页签通过同源 iframe 嵌入，规则查询接口允许普通用户只读；所有修改、扫描、发布和停用 POST 仍要求管理员会话。
- 正式 `/` 入口不引用 `workbench-prototype.*`。自动化验证：`python -m unittest discover -s tests -v` 共 137 项通过；`node --check` 与 `py_compile` 通过；应用内浏览器完成桌面、720×900 窄屏、四页签、多卡、输出分组和规则只读浏览验收。
