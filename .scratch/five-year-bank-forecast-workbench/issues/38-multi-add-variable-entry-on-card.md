# 38 — P0：多输入反推"添加为变量"入口上卡

**What to build:** 修复 multi 模块加变量路径深藏的问题：当前 multi 模块选中指标后，中栏卡片只显示一行提示文字"选择后可添加为变量或约束"（web/app.js:1438），唯一入口藏在"高级"编辑器内的"多输入优先级反推 v2"折叠面板里（app.js:850）。改为：multi 模块下，未加为变量的选中指标卡直接在卡面提供「添加为变量」按钮（点击即 `addReverseVariable`，默认 优先级=追加序号、年份=2030、初始值=2030 基准、范围=规则允许范围）；已是变量的卡显示变量配置（现状保留）；同时卡面提供「添加为约束」快捷入口（定位到约束构建器并预选该指标）。重复添加的 alert 保留。

**Blocked by:** 无

**Status:** ready-for-agent

- [ ] multi 模块选中未添加指标 → 卡面直接出现「添加为变量」按钮，点击即加入（默认值同现状）
- [ ] 卡面「添加为约束」点击后约束构建器预选该指标并聚焦
- [ ] single 模块卡片行为不变（变量卡单例逻辑不动）
- [ ] 添加后卡片即刻切换为变量卡形态（优先级/范围/初始值可见可改）
- [ ] `python -m unittest discover -s tests` 全绿；`node --check web/app.js` 通过

## Comments

- 2026-07-20 来源：反向模块走查（agent-13 报告摩擦点 #3）。不翻代码根本不知道能加变量，属阻断级。
- 2026-07-20 注意：ticket 40 会把 v2 面板整个删除并把全字段配置收敛到卡面，本票只加入口按钮，面板去留不动，避免与 40 冲突。
