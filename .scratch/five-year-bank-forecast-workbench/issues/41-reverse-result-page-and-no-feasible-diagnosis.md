# 41 — P1：反向结果页重构——结论横幅、变量建议表、约束满足表、无解诊断

**What to build:** 把反向结果从右栏小卡片堆（`renderReverseResult`，web/app.js:524-545）重构为结构化结果区：
1. **结论横幅**：可行（绿）/ 无解（红）+ 一句话摘要（"搜索 12/25 次 · 软偏差 0.3"），可行时提供"导出反向结果"按钮（复用现有逻辑）。
2. **变量建议表**：指标 | 基准值 | 建议值 | 调整量 | 触边 | 启用原因（`reason`）；v2 按优先级分组展示，未启用变量单列并标注原因。
3. **约束满足表**：约束（含组标签 group_label 与逐年值）| 目标 | 实际 | 偏差 | 命中；未命中行红色高亮。
4. **无解诊断面板**（无解时展示，本票的实用性核心）：按偏差降序列出缺口最大的约束（"净息差 2029：目标 ≥1641，最好只到 1593，差 48"）；触边变量（`hit_boundary`）给建议"已顶到搜索边界，建议放宽范围或降低约束"；全部硬约束无解时提示"可将部分约束转为软目标"；v2 展示 `no_feasible_reason`（后端已返回，前端目前未引用）。
5. 结果区表格化对齐（复用 result-table 样式），指标名不截断（title 悬浮全名），数字沿用 `formatDetailResultValue` 千分位格式。

**Blocked by:** 无

**Status:** ready-for-agent

- [ ] 可行/无解结论横幅 + 搜索摘要一句话
- [ ] 变量建议表（v2 含优先级分组与未启用标注）
- [ ] 约束满足表（未命中高亮，组标签可辨）
- [ ] 无解诊断：偏差排序 + 触边建议 + 转软提示 + v2 no_feasible_reason 展示
- [ ] 数字千分位、指标名悬浮全名
- [ ] 导出反向结果按钮在横幅处可用（可行时）
- [ ] `python -m unittest discover -s tests` 全绿；`node --check web/app.js` 通过

## Comments

- 2026-07-20 来源：反向模块走查（agent-13 报告摩擦点 #10，agent-14 报告 §1/§6）。后端早已返回 actual/deviation/hit_boundary/no_feasible_reason，前端未展示——无解诊断是把已有数据接上线，不碰后端契约与搜索算法。
- 2026-07-20 边界：本票只动结果展示层；v2 调整路径时间线在 #42；运行按钮与预算控制在 #39/#43。
