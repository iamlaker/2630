# 29 — 目录补全：资本假设分组纳入输入指标与规则发现

**What to build:** 把 0717 模板`汇总展示表`第 153–196 行的"资本假设"分组纳入指标目录（含（一）各类业务综合风险权重、（二）表外业务复合增速、（三）风险加权资产、（四）并表资本端变化四个子节，分红率在第 191 行），使其作为输入指标出现在左栏导航；规则发现/确认链路同步覆盖，保证可确认后可编辑。

**Blocked by:** 无（解析层改动，建议单独做）

**Status:** ready-for-agent

- [x] 查明当前目录解析（template_catalog.py 或相邻模块）如何识别"重要参数/规模假设/价格假设/中收假设"四组（大概率按 A 列组标题行分段），让"资本假设"段同样入库，classification=input
- [x] 子节标题行（（一）…（四））按标题类行处理（参考 ticket 31 的标题行机制，两个 ticket 可共用）
- [x] 新增输入指标的规则发现可用；未确认前按现有制度显示"待确认"、不可编辑（与现有 78 项一致的生命周期）
- [x] 已导入的活动模板经 `--rescan-template`（或既有机制）重扫后，左栏出现"资本假设"组；不破坏既有 78 项的规则与已发布状态
- [x] Edge headless 截屏验证左栏出现资本假设组并可展开
- [x] `python -m unittest discover -s tests` 全绿；解析逻辑补测试

## Comments

- 2026-07-19 来源：用户走查反馈第 1(3) 条。模板证据：汇总展示表 153 行组标题"资本假设"，子节在 153/175/182/189 行，分红率在 191 行。注意 PRD 编写时只列了四个输入组，本 ticket 属于范围补全。
- 2026-07-19 实现：根因是 `INPUT_GROUPS` 只列四组——引擎解析本就按 A 列组名分段，资本假设行一直在目录里但被分类为 unknown。修复：INPUT_GROUPS 加入"资本假设"；新增 `TemplateImportService.regenerate_catalog`（保留 template_version_id 原地刷新 catalog_json）并接入 `_rescan_template`（重扫先再生成目录再做规则发现）。对活动模板执行 `--rescan-template 3`：118 版规则 = 78 复用（已确认规则全部保留）+ 40 新增（资本假设，按制度待确认不可编辑）；左栏出现"资本假设 40/40 项"组（CDP 截屏验证）。
